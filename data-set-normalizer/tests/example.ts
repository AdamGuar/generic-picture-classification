
import { expect } from 'chai';

describe('Test tests', () => {
    describe('test()', () => {
        it('should return proper options objects', () => {
            expect(true).to.be.equal(true);
        });
    });
});
